{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "postinstall": "curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer",
        "build": "composer install && npm install && npm run build"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5",
        "@tailwindcss/typography": "^0.5",
        "autoprefixer": "^10.4",
        "laravel-vite-plugin": "^1.0",
        "resolve-url-loader": "^5.0.0",
        "sass": "^1.3",
        "sass-loader": "^8.0",
        "tailwindcss": "^3.4",
        "vite": "^5.0"
    }
}
